
# This is a sample inventory file for Linux hosts in lab1
lab1_linux_hosts:
  hosts:
    host-1:
      ansible_host: host1.local   # Replace with actual hostnames or IP addresses

  vars:
    ansible_user: remote-user-with-ssh-key-authentication-enabled  # Default user for SSH connections